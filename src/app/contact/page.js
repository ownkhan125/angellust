import ContactPage from '@/components/pages/contact-page'

export const metadata = {
  title: 'Contact · AngelLust for Congress',
  description:
    'Send a message to the campaign — every reply comes from someone on our team, not a bot.',
}

export default function Page() {
  return <ContactPage />
}
